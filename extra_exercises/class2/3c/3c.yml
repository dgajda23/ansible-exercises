---
- name: Exercise 3c
  hosts: nxos
  gather_facts: False
  tasks:
    - name: Execute commands  on device
      cisco.nxos.nxos_command:
        commands: 
          - show version
          - show lldp neighbors 
      register: device_output

    - name: Print device output
      debug:
        var: device_output['stdout_lines'][1]
