---
- name: Exercise 2c
  hosts: cisco
  gather_facts: False
  tasks:
    - name: Print BGP ASN and loopback0 for cisco hosts
      debug:
        msg: "The ASN for host {{ inventory_hostname }} is {{ bgp_asn }}, the router-id is {{ loopback0 }}"

