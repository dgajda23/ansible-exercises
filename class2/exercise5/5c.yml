---
- name: Exercise 5a
  hosts: juniper
  gather_facts: False
  tasks:
    - name: Run the show interfaces terse command
      junipernetworks.junos.junos_command: 
        commands: show interfaces terse
      register: interfaces

    - ansible.builtin.debug:
        msg: "{{ interfaces['stdout_lines'][0][21].split('  ')[-1] }}" 

